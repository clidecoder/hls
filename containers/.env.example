# GitHub Container Registry settings
GITHUB_USERNAME=your-github-username

# Domain configuration
DOMAIN_NAME=your-domain.com

# GitHub API configuration
GITHUB_TOKEN=your-github-personal-access-token
GITHUB_WEBHOOK_SECRET=your-webhook-secret

# Claude AI configuration
# For Claude Code CLI, use 'claude-code' or leave empty
# For API access, use your Anthropic API key
ANTHROPIC_API_KEY=claude-code

# Optional: Override default ports
# HLS_PORT=8000
# HLS_WEBHOOK_PATH=/webhook

# Shared state directory (defaults to ./shared_state)
# Useful for custom backup locations or network storage
# SHARED_STATE_DIR=./shared_state